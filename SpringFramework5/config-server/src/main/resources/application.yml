spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: /home/mahesh/platform/AxonWS/config-files/
server:
  port: 8888